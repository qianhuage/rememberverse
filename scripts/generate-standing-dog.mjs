import {readFileSync,writeFileSync} from 'node:fs';
const key=readFileSync('.dev.vars','utf8').split('\n').find(l=>l.startsWith('MINT_API_KEY=')).slice(13).replace(/^['"]|['"]$/g,'');
const headers={Authorization:`Bearer ${key}`,'Content-Type':'application/json'};
const up=await fetch('https://api.mint.gg/v1/reference-images',{method:'POST',headers,body:JSON.stringify({base64Data:readFileSync('public/demo/dog-standing-reference.png').toString('base64'),fileName:'demo-dog-standing.png',contentType:'image/png',name:'Demo dog reference'})}).then(r=>r.json());
if(!up.url)throw Error('Reference upload failed');
const result=await fetch('https://api.mint.gg/v1/models:generate',{method:'POST',headers:{...headers,'Idempotency-Key':'metacemetery-user-dog-standing-001'},body:JSON.stringify({name:'Golden dog companion',imageUrl:up.url,prompt:'Create this exact golden tan dog as a beautiful realistic stylized game character. IMPORTANT change pose to standing on all four straight legs separated clearly, neutral quadruped rest pose suitable for animation rigging, facing forward. Preserve stocky fluffy body, broad short muzzle, black nose, small dark brown folded ears, golden ochre coat, creamy chest, white toes, thick curled tail. Isolated dog only no ground, no background. Full body textured mesh.',generationMode:'auto',generationPreset:'standard'})}).then(r=>r.json());writeFileSync('work/dog-standing-operation.json',JSON.stringify(result));console.log(JSON.stringify(result).slice(0,1100));
